{
  "name": "index",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js npx puppeteer browsers install chrome",
    "build": "node index.js npx puppeteer browsers install chrome",
    "dev": "nodemon index.js npx puppeteer browsers install chrome"
  },
  "author": "Hitish",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "puppeteer": "^22.6.3",
    "nodemon": "^3.1.0"
  }
}
